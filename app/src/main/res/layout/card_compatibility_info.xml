<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/card_compatibility_bg"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginBottom="8dp"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/big_tile_bg"
        android:contentDescription="@string/image" />

    <ImageView
        android:id="@+id/card_compatibility_close"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginBottom="70dp"
        android:layout_marginEnd="12dp"
        android:layout_marginTop="12dp"
        android:contentDescription="@string/image"
        android:tint="@color/minimal_green"
        app:layout_constraintBottom_toBottomOf="@+id/card_compatibility_bg"
        app:layout_constraintEnd_toEndOf="@+id/card_compatibility_bg"
        app:layout_constraintTop_toTopOf="@+id/card_compatibility_bg"
        app:srcCompat="@drawable/ic_close_round" />

    <TextView
        android:id="@+id/card_compatibility_desc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="2dp"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="16dp"
        android:text="@string/info_card_compatibility_msg"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="@+id/card_compatibility_bg"
        app:layout_constraintEnd_toStartOf="@+id/card_compatibility_close"
        app:layout_constraintStart_toStartOf="@+id/card_compatibility_bg"
        app:layout_constraintTop_toTopOf="@+id/card_compatibility_bg" />

</android.support.constraint.ConstraintLayout>